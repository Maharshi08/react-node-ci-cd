services:
  mongo-prod:
    image: mongo:6
    networks:
      - app-network
    restart: unless-stopped

  backend-prod:
    image: ${DOCKERHUB_NAMESPACE:-maharshi86}/react-node-ci-backend:${IMAGE_TAG}
    build:
      context: ./backend
      dockerfile: Dockerfile
    env_file:
      - ./.env.prod
    ports:
      - "5001:5000"
    depends_on:
      - mongo-prod
    networks:
      - app-network
    restart: unless-stopped

  frontend-prod:
    image: ${DOCKERHUB_NAMESPACE:-maharshi86}/react-node-ci-frontend:${IMAGE_TAG}
    build:
      context: ./frontend
      dockerfile: Dockerfile
    depends_on:
      - backend-prod
    networks:
      - app-network
    restart: unless-stopped

  nginx-prod:
    image: ${DOCKERHUB_NAMESPACE:-maharshi86}/react-node-ci-nginx:${IMAGE_TAG}
    build:
      context: ./nginx
      dockerfile: Dockerfile
    ports:
      - "80:80"
    depends_on:
      - frontend-prod
      - backend-prod
    networks:
      - app-network
    restart: unless-stopped

networks:
  app-network:
    driver: bridge
